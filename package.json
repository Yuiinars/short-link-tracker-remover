{
  "name": "short-link-tracker-remover",
  "version": "1.0.0",
  "description": "A high-performance, TypeScript-based API that swiftly intercepts and sanitizes short links, removing tracking parameters.",
  "type": "module",
  "author": "Yuiinars",
  "main": "dist/server.js",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build ./src/server.ts --outdir ./dist",
    "start": "bun run dist/server.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.28.1",
    "got": "^14.4.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
